#!/usr/bin/env sh
set -eu; cd "$(dirname "$0")/.."
if command -v docker >/dev/null; then docker compose up --build; else echo 'Docker missing; starting desktop preview only.' >&2; pnpm dev:desktop; fi
