#!/usr/bin/env sh
set -eu; cd "$(dirname "$0")/.."; pnpm lint; pnpm test; pnpm build; pnpm run license-notices
echo 'Use scripts/package-release.ps1 on Windows or implement the equivalent archive step for the target CI runner.'
