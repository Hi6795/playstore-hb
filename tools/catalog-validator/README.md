# Catalog validator

The validator is exposed through `pnpm publisher validate <catalog>`. It uses the same strict validation code as the client and publication workflow, including cross-record duplicate and production-development checks.
